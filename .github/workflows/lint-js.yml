name: lint-js

on:
  push:
    branches:
      - master
      - dist
  workflow_dispatch:

jobs:
  build:
    name: Lint JS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install NPM deps
        run: npm ci

      - name: Run check
        run: npm run lint
