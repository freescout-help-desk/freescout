if (typeof(LangMessages) == "undefined") {
	LangMessages = {};
}
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["en.messages"]) == "undefined") {
		LangMessages["en.messages"] = {};
	}
	LangMessages["en.messages"] = $.extend(locale_messages, LangMessages["en.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["nl.messages"]) == "undefined") {
		LangMessages["nl.messages"] = {};
	}
	LangMessages["nl.messages"] = $.extend(locale_messages, LangMessages["nl.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["fr.messages"]) == "undefined") {
		LangMessages["fr.messages"] = {};
	}
	LangMessages["fr.messages"] = $.extend(locale_messages, LangMessages["fr.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["de.messages"]) == "undefined") {
		LangMessages["de.messages"] = {};
	}
	LangMessages["de.messages"] = $.extend(locale_messages, LangMessages["de.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["ja.messages"]) == "undefined") {
		LangMessages["ja.messages"] = {};
	}
	LangMessages["ja.messages"] = $.extend(locale_messages, LangMessages["ja.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["it.messages"]) == "undefined") {
		LangMessages["it.messages"] = {};
	}
	LangMessages["it.messages"] = $.extend(locale_messages, LangMessages["it.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["pl.messages"]) == "undefined") {
		LangMessages["pl.messages"] = {};
	}
	LangMessages["pl.messages"] = $.extend(locale_messages, LangMessages["pl.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["pt-PT.messages"]) == "undefined") {
		LangMessages["pt-PT.messages"] = {};
	}
	LangMessages["pt-PT.messages"] = $.extend(locale_messages, LangMessages["pt-PT.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["pt-BR.messages"]) == "undefined") {
		LangMessages["pt-BR.messages"] = {};
	}
	LangMessages["pt-BR.messages"] = $.extend(locale_messages, LangMessages["pt-BR.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["ru.messages"]) == "undefined") {
		LangMessages["ru.messages"] = {};
	}
	LangMessages["ru.messages"] = $.extend(locale_messages, LangMessages["ru.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["es.messages"]) == "undefined") {
		LangMessages["es.messages"] = {};
	}
	LangMessages["es.messages"] = $.extend(locale_messages, LangMessages["es.messages"]);
	var locale_messages = {
        
        "noreply_alert": "Sending to :email email. Are you sure?"
	};

   	if (typeof(LangMessages["sv.messages"]) == "undefined") {
		LangMessages["sv.messages"] = {};
	}
	LangMessages["sv.messages"] = $.extend(locale_messages, LangMessages["sv.messages"]);

(function () {
	if (typeof(Lang) == "undefined") {
    	Lang = new Lang();
    }
    Lang.setMessages(LangMessages);
})();